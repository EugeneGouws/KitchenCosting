# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-ssr/

# Environment variables
.env
.env.local
.env.*.local
# NOTE: wrangler.toml is deliberately NOT ignored — Cloudflare Workers Builds reads it
# from the repo, and ignoring it is what left the app with no deploy config at all.
# It holds no secrets; those are set with `wrangler secret put`.

# Local-only project folders
recipeParser/
docs/
legacy/
CLAUDE.md
.claude/
tests/

# Cloudflare Wrangler local state
.wrangler/

# Local input for scripts/push-prices.js (developer-only price batch)
scripts/new-prices.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo
